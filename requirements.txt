# Core ML dependencies
torch>=2.0.0,<3.0.0
transformers>=4.35.0,<5.0.0  # Updated for PEFT support
tokenizers>=0.13.0,<1.0.0
sentencepiece>=0.1.99,<1.0.0

# PEFT and quantization support
peft>=0.6.0,<1.0.0
bitsandbytes>=0.41.0,<1.0.0
accelerate>=0.24.0,<1.0.0

# Data processing
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
PyMuPDF>=1.23.0,<2.0.0  # fitz
docx2txt>=0.8,<1.0.0
python-pptx>=0.6.21,<1.0.0  # For PPTX support
markdown>=3.4.0,<4.0.0
beautifulsoup4>=4.12.0,<5.0.0

# Enhanced document processing
ebooklib>=0.18,<1.0.0  # EPUB support
pytesseract>=0.3.10,<1.0.0  # OCR support
Pillow>=9.0.0,<11.0.0  # Image processing for OCR
requests>=2.28.0,<3.0.0  # HTTP requests for HTML processing

# Deduplication dependencies
sentence-transformers>=2.2.0,<3.0.0  # Embedding-based deduplication
scikit-learn>=1.3.0,<2.0.0  # Similarity calculations
xxhash>=3.0.0,<4.0.0  # Fast hashing (optional)

# Language detection
langdetect>=1.0.9,<2.0.0  # Language detection for filtering

# Testing dependencies (optional)
reportlab>=3.6.0,<5.0.0  # PDF creation for testing

# CLI and UI dependencies
click>=8.0.0,<9.0.0  # CLI framework
rich>=13.0.0,<14.0.0  # Rich text and progress bars (fallback)
colorama>=0.4.6,<1.0.0  # Cross-platform colored terminal text

# Utilities
tqdm>=4.65.0,<5.0.0
PyYAML>=6.0,<7.0.0
loguru>=0.7.0,<1.0.0
jsonschema>=4.17.0,<5.0.0  # Configuration validation
psutil>=5.9.0  # System monitoring

# Optional GPU support (can be installed separately)
# torch[cuda] for CUDA support
# Add when GPU training is needed
